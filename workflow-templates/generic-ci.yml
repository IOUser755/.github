name: Generic CI
on: [push, pull_request]
jobs:
  discover:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Docs-only repo; no language-specific jobs detected."
